import 'package:flutter/material.dart';

class EditMypageScreen extends StatelessWidget {
  const EditMypageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('마이페이지 수정 화면')));
  }
}
