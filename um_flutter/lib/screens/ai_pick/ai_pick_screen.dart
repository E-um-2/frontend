import 'package:flutter/material.dart';

class AIPickScreen extends StatelessWidget {
  const AIPickScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('AI추천 화면')));
  }
}
