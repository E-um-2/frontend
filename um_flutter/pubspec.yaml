name: um_test
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  go_router: ^13.0.1
  cupertino_icons: ^1.0.8
  google_maps_flutter: ^2.5.3
  location: ^5.0.3
  http: ^1.4.0
  flutter_dotenv: ^5.2.1
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/bike_stations.json
    - .env
    - assets/landmarks.json

